project_name: notionmd-cli

builds:
  - main: ./main.go
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    binary: notionmd-cli
    ldflags: -X main.Version={{.Version}} -s -w

archives:
  - format: tar.gz

release:
  github:
    owner: christhomas
    name: notionmd-cli